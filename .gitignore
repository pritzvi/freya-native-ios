# macOS
.DS_Store

# Xcode user data
xcuserdata/
*.xcuserstate

# Build folders
build/
DerivedData/

# Swift Package Manager
.swiftpm/
Package.resolved

# CocoaPods (ignore if not used)
Pods/

# Node / JS (ignore everywhere)
node_modules/
npm-debug.log*
yarn-error.log*
pnpm-debug.log*

# Env files (ignore everywhere; keep sample)
.env
.env.*
!.env.example

# Firebase (backend/emulators)
.firebase/
.emulator-cache/
firebase-debug.log
.firebaserc.local